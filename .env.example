# Copia este archivo como .env.local y rellena los valores.
# .env.local NO se sube a git.

# Supabase: proyecto y clave publicable (la que empieza por sb_publishable_ o eyJ).
# Es la clave pensada para el navegador; la seguridad está en las políticas RLS
# de la tabla, no en ocultarla. Ver supabase/solicitudes.sql
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
