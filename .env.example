# Copia este archivo como .env.local y rellena los valores.
# .env.local NO se sube a git.
#
# Clave PUBLICABLE de Stripe (empieza por pk_). Puede ir en el navegador.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# La clave SECRETA (sk_) solo hace falta si se monta pago con código.
# Con un enlace de pago de Stripe no se necesita ninguna clave.
